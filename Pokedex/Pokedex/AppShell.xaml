<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Pokedex.Views;assembly=Pokedex"
       BackgroundColor="{DynamicResource Primary}"
       Shell.FlyoutBackgroundColor="{DynamicResource Tertiary}"
       TabBarBackgroundColor="{DynamicResource Secondary}"
       ForegroundColor="{DynamicResource LightPrimary}"
       TabBarTitleColor="{DynamicResource LightPrimary}"
       TabBarUnselectedColor="{DynamicResource DarkPrimary}"
       
       x:Class="Pokedex.AppShell">

    <TabBar>
        <Tab Title="Pokemons"
             Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate views:PokemonListView}" />
        </Tab>        
        <Tab Title="Pokemons"
             Icon="home.png">
            <ShellContent ContentTemplate="{DataTemplate views:PokemonListView}" />
        </Tab>
        <Tab Title="Pokemons"
             Icon="monkey.png">
            <ShellContent ContentTemplate="{DataTemplate views:PokemonListView}" />
        </Tab>
        
    </TabBar>

</Shell>
